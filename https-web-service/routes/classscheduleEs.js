/*Es modules and commonJS format
common js is pretty much old why still we use common gs on noad is because to have a backward compatibility
ECMAScript is a standard for scripting languages,including JavaScript,JScript , and ActionScript.
promises,or synchronous programming , mapping and for each.filter handled ecmascript.
 */
import express from 'express';
import fs from 'fs';
import data from '../database/class-schedule.json'




export default classscheduleEs